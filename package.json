{
    "name": "note-taking-app",
    "private": true,
    "version": "1.0.0",
    "scripts": {
        "dev": "concurrently \"cd server && node --env-file=config.env server\" \"cd client && npm run dev\""
    },
    "devDependencies": {
        "concurrently": "^7.0.0"
    },
    "dependencies": {
        "bcrypt": "^5.1.1",
        "node": "^23.8.0",
        "react-icons": "^5.3.0"
    }
}
